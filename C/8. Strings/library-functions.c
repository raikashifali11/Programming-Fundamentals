#include<stdio.h>
#include<string.h>

int main(){
    // 1-count string length

    // char name[] = "Rai Kashif Ali";
    // int length = strlen(name);
    // printf("length is %d",length);



    // 2-copy string

    // char oldStr[] = "old is gold"; 
    // char newStr[] = "new world";
    // strcpy(oldStr,newStr);
    // puts(oldStr);




    // 3-concatenates string

    // char firstStr[] = "Hello ";
    // char secondStr[] = "World";
    // strcat(firstStr,secondStr);
    // puts(firstStr);

    


    // 4-String comparison

    // char firstStr[] = "apple";
    // char secondStr[] = "banana";
    // printf("comparison is %d\n",strcmp(firstStr,secondStr));

    return 0;
}